<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/mobile_navigation"
    app:startDestination="@+id/navigation_home">

    <fragment
        android:id="@+id/navigation_home"
        android:name="pedroluiz.projeto.soccernews.presentation.news.NewsFragment"
        android:label="@string/title_new"
        tools:layout="@layout/fragment_news" />

    <fragment
        android:id="@+id/navigation_favorito"
        android:name="pedroluiz.projeto.soccernews.presentation.favoritos.FavoriteFragment"
        android:label="@string/title_favoritos"
        tools:layout="@layout/fragment_favorite" />

</navigation>